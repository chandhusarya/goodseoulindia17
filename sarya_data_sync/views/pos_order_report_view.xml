<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_pos_order_report_pivot" model="ir.ui.view">
        <field name="name">report.pos.consumption.pivot</field>
        <field name="model">pos.order.report</field>
        <field name="arch" type="xml">
            <pivot string="Point of Sale Order Analysis" sample="1">
                <field name="product_categ_id" type="row"/>
                <field name="date" interval="month" type="col"/>
                <field name="order_id" type="measure"/>
                <field name="product_qty" type="measure"/>
                <field name="price_total" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="view_pos_order_report_graph" model="ir.ui.view">
        <field name="name">pos.order.report.graph</field>
        <field name="model">pos.order.report</field>
        <field name="arch" type="xml">
            <graph string="Point of Sale Order Analysis" sample="1">
                <field name="product_categ_id"/>
                <field name="price_total" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="report_pos_order_report_view_tree" model="ir.ui.view">
        <field name="name">pos.order.report.view.tree</field>
        <field name="model">pos.order.report</field>
        <field name="arch" type="xml">
            <tree string="Point of Sale Order Analysis">
                <field name="date" widget="date"/>
                <field name="order_id" optional="hide"/>
                <field name="partner_id" optional="hide"/>
                <field name="product_id" optional="show"/>
                <field name="product_categ_id" optional="show"/>
                <field name="config_id" optional="hide"/>
                <field name="company_id" optional="show" groups="base.group_multi_company"/>
                <field name="price_total" optional="show"/>
                <field name="state" optional="show"/>
            </tree>
        </field>
    </record>

    <record id="view_pos_order_report_search" model="ir.ui.view">
        <field name="name">pos.order.report.search</field>
        <field name="model">pos.order.report</field>
        <field name="arch" type="xml">
            <search string="Point of Sale Order Analysis">
                <field name="date"/>
                <separator/>
                <filter string="Invoiced" name="invoiced" domain="[('state','=',('invoiced'))]"/>
                <filter string="Not Invoiced" name="not_invoiced" domain="[('state','in',['paid', 'done'])]"/>
                <separator/>
                <filter string="Combo Items" name="not_invoiced" domain="[('combo_parent_line_id', '!=', False)]"/>
                <filter string="Menu Items" name="not_invoiced" domain="[('combo_parent_line_id', '=', False)]"/>
                <separator/>
                <filter name="filter_date" date="date"/>
                <field name="config_id"/>
                <field name="partner_id"/>
                <field name="product_id"/>
                <field name="product_categ_id"/>
                <group expand="1" string="Group By">
                    <filter string="User" name="User" context="{'group_by':'user_id'}"/>
                    <filter string="Point of Sale" name="pos" context="{'group_by':'config_id'}"/>
                    <filter string="Product" name="product" context="{'group_by':'product_id'}"/>
                    <filter string="Product Category" name="product_category"
                            context="{'group_by':'product_categ_id'}"/>
                    <separator/>
                    <filter string="Order Date" name="order_month" context="{'group_by':'date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_pos_order_report" model="ir.actions.act_window">
        <field name="name">POS Order Report</field>
        <field name="res_model">pos.order.report</field>
        <field name="view_mode">tree,graph,pivot</field>
        <field name="search_view_id" ref="view_pos_order_report_search"/>
        <field name="context">{'group_by_no_leaf':1,'group_by':[]}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No data yet!
            </p>
            <p>
                Create a new POS order
            </p>
        </field>
    </record>

</odoo>