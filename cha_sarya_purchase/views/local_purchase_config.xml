<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Form View -->
        <record id="view_local_purchase_config_form" model="ir.ui.view">
            <field name="name">local.purchase.config.form</field>
            <field name="model">local.purchase.config</field>
            <field name="arch" type="xml">
                <form string="Local Purchase Configuration">
                    <sheet>
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only"/>
                            <h1><field name="name"/></h1>
                        </div>
                        <group>
                            <group>
                                <field name="operation_type_id" options="{'no_create': True}"/>
                                <field name="analytic_account_id" options="{'no_create': True}"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Tree View -->
        <record id="view_local_purchase_config_tree" model="ir.ui.view">
            <field name="name">local.purchase.config.tree</field>
            <field name="model">local.purchase.config</field>
            <field name="arch" type="xml">
                <tree string="Local Purchase Configuration">
                    <field name="name"/>
                    <field name="operation_type_id"/>
                    <field name="analytic_account_id"/>
                </tree>
            </field>
        </record>

        <!-- Action -->
        <record id="action_local_purchase_config" model="ir.actions.act_window">
            <field name="name">Local Purchase Config</field>
            <field name="res_model">local.purchase.config</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new Local Purchase Configuration
                </p>
            </field>
        </record>

        <!-- Menu Item -->
        <menuitem id="menu_local_purchase_config"
                  name="Local Purchase Config"
                  parent="purchase.menu_purchase_config"
                  action="action_local_purchase_config"
                  sequence="100"/>

    </data>
</odoo>
